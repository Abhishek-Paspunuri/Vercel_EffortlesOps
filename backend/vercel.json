{
    "version":2,
    "builds":[
        {
            "src":"./index.js",
            "use":"@vercel/node"
        }
    ],
    "build": {
        "base": "https://github.com/heroku/heroku-buildpack-apt.git#v54"
    },
    "routes":[
        {
            "src":"/(.*)",
            "dest":"/"
        }
    ]
}